{
    "version":2,
    "installCommand":"npm install && npm run server",
    "builds":[
        {
          "src":"./server.js",
          "use":"@vercel/node"
        }
    ],
    "routes":[
        {"src":"/(.*)","dest":"/"}
    ]
}